# DuckDB
*.duckdb
*.duckdb.wal

# SQLite (transient files only; rav.db is tracked as seed database)
*.db-journal
*.db-wal

# Node
node_modules/
dist/

# Python (no project scripts — ignore local cache/venvs if you run Python here)
__pycache__/
*.pyc
*.pyo
.mypy_cache/
.pytest_cache/
.ruff_cache/
.venv/
venv/

# Environment / secrets
.env
.env.*

# OS
.DS_Store
Thumbs.db

# Editor
*.swp
*.swo
*~

# Data (raw fetched data is large but tracked intentionally)
# 1_data_sources/snap_ads/data/
